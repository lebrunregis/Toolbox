using UnityEngine;

namespace Misc.Editor
{
    public class ToolbarShortcuts : MonoBehaviour
    {
        // Start is called before the first frame update
        private void Start()
        {

        }

        // Update is called once per frame
        private void Update()
        {

        }
    }
}
